spring:
  main:
    banner-mode: off

  datasource:
    url: jdbc:postgresql://10.10.14.13:5432/BankApplication-dev
    username: bankapplication
    password: bankapplication

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none

  liquibase:
    change-log: classpath:/db/changelog/changelog-master.xml

app:
  description: ${description}
  version: ${version}
  server:
    url: ${app.server.url}